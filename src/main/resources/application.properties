spring.application.name=ExpenseTracker
server.port=8081

# MySQL datasource
spring.datasource.url=jdbc:mysql://192.168.0.106:3306/expense_tracker?useSSL=false
spring.datasource.username=pi
spring.datasource.password=pi200997
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=expensetracker.app.test@gmail.com
spring.mail.password=znbryslfbzifnpex
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Enable debug logging for JavaMail to see SMTP conversation
spring.mail.properties.mail.debug=true

# Log levels to help troubleshoot mail sending and our MailService
logging.level.org.springframework.mail=DEBUG
logging.level.org.springframework.web=INFO
logging.level.com.expensetracker.service.MailService=DEBUG

# Configure Jackson to write dates as ISO strings rather than timestamps/arrays
spring.jackson.serialization.write-dates-as-timestamps=false

# Optionally set date/time format (the DTOs use @JsonFormat; this is a fallback)
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss

# CORS allowed origins/patterns (comma-separated). Default includes local testing URL pattern and prod URL. Override with env var APP_CORS_ALLOWED_ORIGINS in deployed environments.
app.cors.allowed-origins=http://localhost:*,https://expensetracker.eternivity.com
